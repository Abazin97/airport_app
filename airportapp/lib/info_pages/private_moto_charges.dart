import 'package:flutter/material.dart';

class PrivateMotoCharges extends StatelessWidget {
  const PrivateMotoCharges({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}