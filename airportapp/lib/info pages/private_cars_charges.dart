import 'package:flutter/material.dart';

class PrivateCarsCharges extends StatelessWidget {
  const PrivateCarsCharges({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}